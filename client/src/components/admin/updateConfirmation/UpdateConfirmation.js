import React from "react";

const UpdateConfirmation = ({ className }) => {
  return (
    <p className={`${className} admin__form-updated-confirmation`}>
      Updated successfully!
    </p>
  );
};

export default UpdateConfirmation;
